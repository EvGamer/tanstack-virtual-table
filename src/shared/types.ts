export type HasId = { id: number };

export type Person = {
  id: number;
  name: string;
  surname: string;
  patronim: string;
  isMale: boolean;
  maritalStatus: string;
  age: number;
  occupation: string;
}